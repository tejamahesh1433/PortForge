# Cross-component tests

Reserved for integration/end-to-end tests that span multiple components
(agent + backend + frontend + cli). Phase 1 unit tests for the agent live in
`agent/tests/` alongside the package they test.
