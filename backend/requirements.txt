fastapi>=0.116
uvicorn[standard]>=0.35
sqlalchemy>=2.0
alembic>=1.16
psycopg[binary]>=3.1
pydantic>=2.11
pydantic-settings>=2.10

# Shared domain value types (Protocol/PortState/Confidence) -- see
# app/models/base.py's module docstring for why the central server
# depends on the agent library rather than a third duplicate package.
# Installed explicitly in Dockerfile; for local dev see requirements-dev.txt
